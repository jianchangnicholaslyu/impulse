{
  "name": "impulse-marketplace",
  "version": "0.14.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "find api src -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "devDependencies": {
    "vite": "^8.0.13"
  },
  "dependencies": {
    "resend": "^6.12.3"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  }
}
